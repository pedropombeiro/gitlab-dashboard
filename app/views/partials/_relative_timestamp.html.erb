<% if local_assigns[:timestamp] %>
  <abbr data-bs-toggle="tooltip" data-bs-title="<%= timestamp %>"><%= humanized_duration(Time.now - timestamp, most_significant_only: local_assigns[:most_significant_only] || false) %></abbr></caption>
<% else %>
  N/A
<% end %>
