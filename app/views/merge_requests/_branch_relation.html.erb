<%= render "branch_ref", branch_name: source_branch %>

<% if %w[master main].exclude?(target_branch) %>
  <div class="d-inline-flex align-items-center">
    <i class="fa-solid fa-arrow-right text-success"></i>&nbsp;
    <% if @response[:open_merge_requests].any? { |other_mr| other_mr.sourceBranch == target_branch && other_mr.autoMergeEnabled } %>
      <%= render "abbr_with_title", content: "🚀", title: "Auto-merge is enabled" %>
    <% end %>
    <%= render "branch_ref", branch_name: target_branch %>
  </div>
<% end %>
