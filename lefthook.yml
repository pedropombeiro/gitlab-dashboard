pre-push:
  parallel: true
  commands:
    backend-specs:
      glob: "{app,spec}/**/*.rb"
      run: bin/rails spec

pre-commit:
  commands:
    backend-linter:
      glob: "*.rb"
      run: bin/rake standard {all_files}
