#!/usr/bin/env bash
#MISE description="Run hk fixers on files"
#USAGE arg "[files]" var=#true help="Specific files to fix (default: all files)"

set -euo pipefail

eval "files=(${usage_files:-})"
if [ ${#files[@]} -gt 0 ]; then
  hk fix "${files[@]}"
else
  hk fix --all
fi
